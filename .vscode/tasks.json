{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Validate Roo Custom Modes",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "find \"${workspaceFolder}/custom_modes.d\" -name '*.yaml' -print0 | xargs -0 -n1 python3 \"${workspaceFolder}/scripts/validate_custom_modes.py\" || exit 1"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": {
        "owner": "roo-custom-modes",
        "fileLocation": ["relative", "${workspaceFolder}"],
        "pattern": [
          {
            "regexp": "^validation error: (.*)$",
            "message": 1
          },
          {
            "regexp": "^error: (.*)$",
            "message": 1
          },
          {
            "regexp": "^YAML parse error in (.*?): (.*)$",
            "file": 1,
            "message": 2
          }
        ]
      },
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "detail": "Validates every YAML in custom_modes.d/ with the Roo schema on demand or when the workspace opens"
    }
  ]
}
